# Handoff — 2026-08-31

Session goal was to find out where a serviced DECD frame stops being video. It
moved further than any previous session on that question, and the answer is now
bounded from both ends: the frame **is** serviced by the firmware, its pixel
content **is** sampled by hardware, and it still does not reach the panel. The
gap that remains is a few hundred registers wide rather than a whole pipeline.

Separately, the milestone the 08-30 handoff left open — "one CALL round-trip
from Linux, with the MIPS alive" — is done, and with it the suppression lead is
closed on a valid observable at last.

Branch `h713-display-video-path`. **Everything below is uncommitted**, including
904 lines of corrections to `patches/kernel/0014-soc-sunxi-add-cpu-comm-ipc.patch`.
See "Repository state" at the end before doing anything else.

## The headline: hardware samples the submitted video content

A red-green-red control, three accepted submissions with identical metadata and
identical DMA addresses, changing only the constant NV12 bytes in the buffer.
Composition-page word `0x05000a60` followed the content in exact A-B-A order:

| frame | `0x05000a60` |
| --- | --- |
| red (`Y=0x51`, `CbCr=0x5a 0xf0`) | `0x0a3c0a3c` |
| green (`Y=0x91`, `CbCr=0x36 0x22`) | `0x06080608` |
| red | `0x0a3c0a3c` |

Green and final-red were each stable across five one-second samples, so these
are settled values, not transients. The panel showed the boot logo throughout,
with no flash or corruption at any point.

**What this closes:** the broad "configured but never fetched" hypothesis. Only
the frame-buffer bytes changed between runs, so something downstream of DECD's
address programming is genuinely reading the pixels.

**What it does not establish:** the word's semantics or its tap position. It
must not be called a CRC, checksum, histogram or final-output monitor yet — that
would be inferring a mechanism from a correlation, which is the specific error
this investigation has had to retract twice. All that is proven is content
dependence.

**What it leaves:** a much narrower gate. Video is fetched and sampled, and the
logo is simultaneously unchanged, so the fault is *after* the content-monitor
tap — blend participation, routing past that tap, or final output selection.

Full result: [`reference/video-content-fetch-confirmed-2026-08-31.txt`](reference/video-content-fetch-confirmed-2026-08-31.txt).

## The configured-versus-serviced question is closed

Three firmware boundaries were hooked in an authenticated `display.bin`
(SHA-256 `4380f1b3…`): PanelWinNode's dirty-mask bit-`0x800` branch into frame
programmer `0x8b1a4538`, the source-0 commit store at MIPS `0xba600014`, and
the AFBD dirty store at `0xba60006c`. Markers land in CPU_COMM shared memory,
readable from Linux at `0x4e340040`.

One bounded fmt-0 submit drove the marker from `0` to `0x6103` with dirty mask
`0xffffffff`, node mode `2`, and immediate commit/dirty readbacks both `1`.
Afterwards `0x05600014` read `0` — hardware consumed the commit — while DECD
stayed enabled with valid Y/C and advancing interrupts.

So the Linux submission reaches the firmware worker, passes the node's dirty-bit
gate, runs the real programmer, and produces a hardware-consumed commit. **Black
output cannot be attributed to an unserviced channel.**

One ordering detail is still unexplained and is a narrow future trace target:
the programmer commits control `0x03000010`, but the delayed read is
`0x03000013`, so something outside the instrumented programmer sets enable
bits 1:0. Earlier hardware already showed that setting and committing enables
1, 2 and 3 by hand still gives black, so this is unlikely to be the fix.

Full result: [`reference/frame-service-gate-confirmed-2026-08-31.txt`](reference/frame-service-gate-confirmed-2026-08-31.txt).

## The firmware-owned composition page, captured for the first time

The ARM `0x05000000` block had never been read on either side. It is now
captured at the accepted-frame boundary, and it settles the 852x480 question
downstream as well as inside AFBD: submitting the old source-coordinate client
changed fourteen size fields across the page from 1280x720 to 852x480, and the
corrected canonical-coordinate client reversed **every one** of them.

A clean control then made the corrected client the first and only submit of a
fresh boot. All geometry and routing words through `+0x87c` stayed
byte-identical at 1280x720, `0x05000030` and `0x05000040` included. Only two
words changed:

```
0x05000058   0x80040000 -> 0x00040000
0x05000104   0x1b00ff00 -> 0x1100ff00
```

They are therefore normal first-frame effects, not residue from the earlier
wrong-coordinate submit. `PanelWinNode::update` at `0x8b1a48cc` contains no
direct access to either offset, so **do not describe them as missing enables** —
they may be hardware status or commit side effects, or work done on another
path. Their meaning is open.

Extending the capture to the full 4 KiB also exposed live telemetry at
`+0x630..+0x648`, `+0x940..+0x958` and `+0x9d8..+0xae0`. `+0xa04`/`+0xa08`
change between samples a second apart. These need semantics before they can be
cited as evidence of anything.

Full diff, apparatus hashes and the watcher source:
[`reference/frame-composition-block-capture-2026-08-31.txt`](reference/frame-composition-block-capture-2026-08-31.txt).

## CPU_COMM works from Linux, and the suppression lead is finally closed

The 08-30 handoff ended with the transport built but no message ever exchanged.
`/dev/cpu_comm` now completes full round trips — CALL → CALL_ACK → RETURN →
RETURN_ACK — reproducibly in ~17–30 ms with zero WARN/BUG, and the driver
discovers the firmware channel itself at probe.

### The argument ABI was wrong, and the U-Boot experiment could not have caught it

The 08-30 U-Boot calls passed a leading `0` then addresses. That made each
adapter's **first** output pointer null, so the address believed to be `p1` was
actually receiving the second output. Linux wire capture settled the indexing:
the adapter is handed the message at `+0x28` (where the component id lives), so
its loads at `+4`/`+8`/`+12` are the first three payload values at `+0x2c`.
**There is no dummy parameter.**

| routine | real signature | returns |
| --- | --- | --- |
| `GetSource` | no arguments | source `0` |
| `Wce_GetActiveWindow` | **two** ARM physical out-addresses | 1920x1080, then active 1280x720 |
| `Wce_GetWindow` | **three** ARM physical out-addresses | two 1920x1080 structs, scalar `2` |

The 1/16-pixel encoding recovered on 08-30 survives unchanged; only the
indexing was wrong. Note the method failure worth carrying forward: a call that
returns successfully with a plausible-looking value is not evidence that the
arguments were interpreted the way you assumed.

That capture also found a driver bug — the channel-addressing path zeroed
message `+0x34`, which is parameter 3, and `CPUComm_CallEx` treated the same
payload word as a tgid before copying arguments over it. Tested fixed module is
SHA-256 `60bbddeab6c985607b7feb7e3d2f15d4e98704016ce8773e10a2b52bdb689608`.

### The three clears, called during live DECD

The 08-30 suppression test was invalid because the observable was an OSD logo
while the routines act on the video channel. That is now fixed: the calls ran
with DECD actively submitting a serviced fmt-0 1280x720 frame.

| call | elapsed | return |
| --- | ---: | --- |
| `b66041d8` `DisableBlackScreen` | 86,121 µs | `nret=0` |
| `3ab1d1dc` `DisableVideoFreeze` | 68,827 µs | `nret=0` |
| `143ffc87` `DisableScreenCover` | 29,600 µs | `nret=0` |

DECD interrupts advanced 75 → 127 across the calls. Every captured register was
identical before and after: source control `0x03000013`, source latch `0`, Y/C
`0x6c500000`/`0x6c5e1000`, video channel `0x00010000`/`0`, OSD channel
`0x03001901`/`0`, mixer `0x00001402`. The hazardous `EnableScreenCover`
(`0152f134`) was not called.

**So the suppression latches are closed as an explanation**, this time on an
observable that could have shown success. One honest limit: the three calls were
compressed into a single short live window, so the operator's visual report
cannot separate them — the IRQ and register evidence does that mechanically.

### `SetSource(1)` is closed too, without repeating the lock-prone loop

A first Linux attempt to call it during live DECD hard-locked the SoC before
the delayed worker printed a line, so neither `GetSource` nor `SetSource` ran.
That is **not** a negative result for `SetSource` — but it is useful evidence
that the MIPS+DECD lock does not require the CPU_COMM module, since it happened
after a successful `FRAME_SUBMIT` return and before the insmod.

A clean project-`0x34` U-Boot trace answered it statically instead: firmware
startup already completes source `0 -> 1` (`VideoDec`, worker marker `0x5203`),
and a later `SetSource(1)` takes the equality branch — accepted, `nret=0`, no
worker transition, no AFBD change. Also worth knowing: `THal_Vp_GetSource`'s
handler at `0x8b14b524` **always returns zero** rather than reading the private
source state, so its `0` was never evidence against this.

The firmware selects VideoDec by itself at startup. Do not re-run `SetSource(1)`
from Linux to "replace" the failed attempt.

## An apparatus correction that nearly cost a result

The first protected composition capture ran `/root/decd-client`, SHA-256
`7c072cfb…` — the **older source-coordinate build**. Reading its live submitted
VideoInfo showed `0x5000`/`0x2d00`, so it reproduced the already-understood
852x480 result rather than testing anything. The corrected binary was already on
the board as `/root/decd-client.coord1080`, SHA-256 `256143c8…`, matching the
hash recorded on 08-30.

**Hash the binary you are about to run against the hash in the document that
justified it.** This is the same class of error as the stale-module measurement
recorded in the client plan, and it is now cost time twice.

## Tools added

- **`tools/display/frame-stage-watch.c`** — the important one. A 3040-byte
  freestanding AArch64 binary that maps the trace and register pages directly,
  busy-polls the stage word, writes one full-page record per stage transition
  through an `O_SYNC` descriptor and `fsync`s each. Run `SCHED_FIFO` 90 on
  CPU 3. It exists because a shell watcher cannot be relied on to be scheduled
  after a risky submit: the first unprotected attempt hard-locked immediately
  and left a zero-length file. Binary SHA-256 `874195f0eb07…`.
- **`tools/display/cpu-comm-probe.c`** — guarded CPU_COMM client. Resolves
  names from `/proc/cpu_comm/routines`, refuses ambiguous prefixes, requires
  `--id` for raw hex, **blacklists `0x0152f134` by resolved id**, caps at ten
  arguments, zeros the result area before every call, and prints elapsed
  microseconds plus symbolic `errno`.
- **`tools/display/mmio-rw.c`** — general 32-bit `/dev/mem` read/write/dump.
  The existing helpers are read-only or bound to a fixed sequence.
- **`tools/display/cc-addr-check.c`** — bench module proving `virt_to_phys()`
  on a module-global takes its `__kimg_to_phys()` branch on arm64 and returns
  an address that is not the data's. Prints a page-table walk beside it.
- **`tools/display/cpu-comm-enable.c`** — one-shot test shim that flips the DT
  `status` on `/soc/cpu-comm@3003000` so the driver can be loaded without
  reflashing. Re-running fails `-ENODEV` because the node stays `OF_POPULATED`;
  to iterate, `rmmod hy310_cpu_comm` and re-insmod the driver alone.

## Operational hazards

Most of these cost time or a result in this session.

- **MIPS + DECD hard-locks, and no ordering cures it.** One lock happened with
  CPU_COMM loaded *before* the submit; the retry survived by submitting first
  and loading CPU_COMM after interrupts were advancing; a later run using that
  same order locked under 500 ms after `FRAME_SUBMIT` returned, before the
  delayed load began. The order reduces one overlap. **Do not keep retrying the
  live-dwell recipe** — design the experiment so the observation survives a lock
  (that is what `frame-stage-watch` is for).
- **After a module Oops, never `reboot`.** `rmmod` sticks in `flush_work` at
  refcount `-1`, and `reboot`/`reboot bootloader` from there hangs with no
  console and no ssh. Use `sync`, then `echo b > /proc/sysrq-trigger`.
- **`sysrq-b` does not sync.** An `scp`'d module still in page cache is lost and
  the board comes back on the *previous* build, which looks exactly like a code
  bug. `sync` first and check the on-board SHA.
- **Shared memory is mapped uncached.** `memcpy` on it takes an alignment fault
  (FSC `0x21`) on arm64 Device memory. Aligned `u32`/`u16` are fine; byte arrays
  need `memcpy_fromio`. Cost one Oops.
- **~90 inherited `BUG()` calls remain** in the cpu_comm sources. Six have been
  fixed and *every one was reached the moment real traffic first touched its
  path*. The `Set*`, notify and callback-registration paths are next and are
  unexercised, not known-good. Read a path's `BUG()`s before running it.
- Standing from earlier sessions and still true: `0x0152f134`
  (`EnableScreenCover`) wedges CPU_COMM and needs a physical power cycle;
  `h713_disp init <id>` means what it says about power-cycling before another
  init.

## Where to go next

**1. Capture `0x05000000..0x05000fff` on stock Android during playback, and
diff it.** This is the highest-value experiment available and it did not exist
before this session. The 08-28 stock block sweep and the 08-29 Linux sweep
covered eleven windows — TVTOP, MIXER, DE_OSD, LAYER, AFBD, ROUTE, LVDS_PHY,
PLL, GE2D, MIPS, IOMMU — and **none of them is `0x05000000`**. The page that
carries the content-following word, and the two first-frame words nobody can
explain, has never been read on a working system.

That is exactly the shape of comparison that produced every real lead on
08-29, and the search is now localised to precisely this page's downstream half.
`/dev/hidtvreg` reads it from stock with no root. Priority targets in the diff:
`0x05000a60` (does stock's follow content the same way?), `0x05000058`,
`0x05000104`, and whatever occupies `+0x9d8..+0xae0`.

**2. Trace what sets `0x05600010` bits 1:0 outside the instrumented programmer.**
Narrow, cheap, and the one loose thread the frame-service trace left. Low
expected value as a fix — hand-setting those enables is already a known negative
— but it completes the picture of who writes that register.

**3. Give the `0x05000000` telemetry words semantics before citing them.**
`+0xa04`/`+0xa08` move on their own. Distinguishing free-running counters from
video-driven state needs the same double-sample method the 08-28 block sweep
used, and until it is done none of that range is usable as evidence.

**4. Only then, more CPU_COMM.** The transport is finished and the routine table
is known, but argument signatures are not — the table publishes names and ids
and nothing else. Read each adapter with `tools/mips/disasm.py` (base
`0x8b100000`, MIPS VA `0x8b1xxxxx` = ARM physical `0x4b1xxxxx`) before calling
anything that sets state. Read-only calls 1–4 in
[`cpu-comm-client-plan.md`](cpu-comm-client-plan.md) are free and safe.

Explicitly **not** worth doing: `SetSource` from Linux (closed above), further
DECD-side format/enable/mux permutations (closed 08-28 by the blue generator),
the mixer (closed 08-29), the AFBD writeback engine (closed 08-30), and
`Wce_SetWindow` (its only real callee is a stub).

## Unfinished documentation

Carried into the next session rather than silently dropped:

- **The raw stage-watch captures live only on the board.** The 08-31 reference
  docs cite them by hash but the files are not in the repo:
  `/root/clean-canonical-first-stage-watch.txt` (3219 lines, SHA-256
  `0703d57f…`), `/root/content-red-stage-watch.txt` (`2014953c…`),
  `/root/content-green-stage-watch.txt` (`162a77b5…`),
  `/root/content-red2-stage-watch.txt` (`334779c7…`). Pull them into
  `docs/reference/` before the board is reflashed — the composition-page diff in
  experiment 1 needs the Linux side of the comparison, and that side is these
  files.
- The 08-30 handoff's "Where to go next" item 2 is complete and is left as
  written; this document supersedes that list.

## Repository state

**Nothing from this session is committed.** Modified: `docs/ge2d-plane-open-re.md`,
`docs/handoff-2026-08-30.md`, `docs/plane-brief-for-external-review.md`,
`docs/reference/cpu-comm-call-table.md`,
`docs/reference/firmware-format-mapping-2026-08-30.md`, `tools/README.md`,
`tools/video/decd-client.c`, `tools/video/decd-enable-test.sh`,
`tools/video/geom-restore-test.sh`, and
`patches/kernel/0014-soc-sunxi-add-cpu-comm-ipc.patch` (904 insertions — the
CPU_COMM driver corrections that made the round trip work; losing these loses
the session's main engineering result).

New and untracked: this file, the three 08-31 reference captures,
`docs/cpu-comm-client-plan.md`, `docs/reference/cpu-comm-routine-table-2026-08-30.txt`,
`docs/reference/osd-hidden-source-enable-2026-08-30.txt`,
`docs/reference/video-coordinate-space-confirmed-2026-08-30.txt`, and the five
tools above plus `tools/display/Kbuild`.

Kernel build artifacts from `tools/display` (`*.ko`, `*.o`, `*.mod*`, `.*.cmd`,
`Module.symvers`, `modules.order`) are now covered by
`tools/display/.gitignore`.
