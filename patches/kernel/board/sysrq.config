# Recovery aid for RAM-only hardware bring-up kernels.  A UART BREAK followed
# by a command character can recover a board from a wedged driver probe without
# requiring a physical power cycle.  It is inert during normal operation.
CONFIG_MAGIC_SYSRQ=y
