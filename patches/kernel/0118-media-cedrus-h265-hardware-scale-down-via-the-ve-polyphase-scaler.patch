media: cedrus: H.265 hardware scale-down via the VE polyphase scaler

The H.265 engine has a secondary output, and mainline carries dead defines for
it that describe a power-of-two shifter like H.264's -- VE_DEC_H265_SDRT_CTRL
with H_SHIFT and V_SHIFT.  Programming it that way hangs the engine, which is
why the secondary output has never been usable here.

That is not how this part scales.  The vendor's decoder picks a scale mode per
picture, and for any real downscale it takes a mode that leaves
reg50.scale_precision at zero and instead programs a different register group:
"VE/Top1 Level", group 7, at VE + 0xf00.  It is a 4-tap/32-phase polyphase
scaler with loadable coefficients, and it lives inside the engine's existing
register window, so it needs no DT change and no separate driver.  Enabling
write_sc_rt_pic without configuring it is exactly the observed timeout: the
engine waits forever on a block that was never told its geometry.

Program it.  Input and output size, a Q12 step and its inverse per axis, a line
buffer, and 64 coefficient words -- horizontal bank first, then vertical.
Coefficients come from cedrus_scaler_coef.h, generated by
tools/video/gen-scaler-coef.py rather than lifted from any vendor binary.

10-bit works too: the secondary output needs its own 2-bit plane, so
OFFSET_ADDR_SECOND_OUT and the SECOND_2BIT fields of VE_DEC_H265_10BIT_CONFIGURE
are programmed alongside.  Those defines already existed and had never been
written.

Measured on hardware, luma PSNR against a software rescale of the same frame:

  1280x720 -> 640x360        55.45 dB
  1280x720 -> 320x180        45.01 dB
  1280x720 -> 640x180        47.85 dB
  Main10 640x480 -> 320x240  53.62 dB   (chroma U 36.10, V 31.15)

For scale, H.264's own shifter scores 46.10 dB on the first of those.  All
seven H.265 conformance vectors in the local set decode clean, scaled and
unscaled, including no-WPP, both scaling-list streams, lossless and Main10;
H.264 scaling is unaffected.

Two things here are not obvious and were each established on hardware, because
reading the vendor binary gave the opposite answer both times:

  - In the size and ratio registers the HIGH half is horizontal and the LOW
    half is vertical.  A symmetric ratio cannot tell the two orders apart, so
    this only shows up on an asymmetric scale.
  - Of the two coefficient banks, the first uploaded drives horizontal and the
    second vertical -- again invisible at a symmetric ratio, where both banks
    hold the same set.

The line buffer is sized from an observed 7680 bytes at a 1280-wide source --
six source lines, the working set of a 4-tap vertical filter.  How that scales
with width is not established, so it is sized for the widest accepted picture
with room to spare; a short buffer faults rather than corrupting quietly.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

--- a/drivers/staging/media/sunxi/cedrus/cedrus.h
+++ b/drivers/staging/media/sunxi/cedrus/cedrus.h
@@ -169,6 +169,8 @@
 			dma_addr_t	neighbor_info_buf_addr;
 			void		*entry_points_buf;
 			dma_addr_t	entry_points_buf_addr;
+			void		*scaler_buf;
+			dma_addr_t	scaler_buf_addr;
 		} h265;
 		struct {
 			unsigned int	last_frame_p_type;
--- a/drivers/staging/media/sunxi/cedrus/cedrus_h265.c
+++ b/drivers/staging/media/sunxi/cedrus/cedrus_h265.c
@@ -25,6 +25,14 @@
  */
 #define CEDRUS_H265_NEIGHBOR_INFO_BUF_SIZE	(794 * SZ_1K)
 #define CEDRUS_H265_ENTRY_POINTS_BUF_SIZE	(4 * SZ_1K)
+/*
+ * Line buffer for the VE+0xf00 scaler.  Observed use is 7680 bytes at a
+ * 1280-wide source -- six source lines, the working set of a 4-tap vertical
+ * filter.  How that scales with width is not established, so size it for the
+ * widest picture the driver accepts with room to spare; a short buffer shows
+ * up immediately as an IOMMU fault rather than as quiet corruption.
+ */
+#define CEDRUS_H265_SCALER_BUF_SIZE		(64 * SZ_1K)
 #define CEDRUS_H265_MV_COL_BUF_UNIT_CTB_SIZE	160
 
 struct cedrus_h265_sram_frame_info {
@@ -862,5 +870,20 @@
 		return -ENOMEM;
 	}
 
+	ctx->codec.h265.scaler_buf =
+		dma_alloc_attrs(dev->dev, CEDRUS_H265_SCALER_BUF_SIZE,
+				&ctx->codec.h265.scaler_buf_addr, GFP_KERNEL,
+				DMA_ATTR_NO_KERNEL_MAPPING);
+	if (!ctx->codec.h265.scaler_buf) {
+		dma_free_coherent(dev->dev, CEDRUS_H265_ENTRY_POINTS_BUF_SIZE,
+				  ctx->codec.h265.entry_points_buf,
+				  ctx->codec.h265.entry_points_buf_addr);
+		dma_free_attrs(dev->dev, CEDRUS_H265_NEIGHBOR_INFO_BUF_SIZE,
+			       ctx->codec.h265.neighbor_info_buf,
+			       ctx->codec.h265.neighbor_info_buf_addr,
+			       DMA_ATTR_NO_KERNEL_MAPPING);
+		return -ENOMEM;
+	}
+
 	return 0;
 }
@@ -901,6 +924,10 @@
 	dma_free_coherent(dev->dev, CEDRUS_H265_ENTRY_POINTS_BUF_SIZE,
 			  ctx->codec.h265.entry_points_buf,
 			  ctx->codec.h265.entry_points_buf_addr);
+	dma_free_attrs(dev->dev, CEDRUS_H265_SCALER_BUF_SIZE,
+		       ctx->codec.h265.scaler_buf,
+		       ctx->codec.h265.scaler_buf_addr,
+		       DMA_ATTR_NO_KERNEL_MAPPING);
 }
 
 static void cedrus_h265_trigger(struct cedrus_ctx *ctx)
--- a/drivers/staging/media/sunxi/cedrus/cedrus_hw.c
+++ b/drivers/staging/media/sunxi/cedrus/cedrus_hw.c
@@ -155,6 +155,109 @@
  * the vendor sets alongside write_sc_rt_pic.  Two outputs writing concurrently
  * is exactly when DDR consistency would matter.
  */
+#include "cedrus_scaler_coef.h"
+
+/*
+ * "VE/Top1 Level" (vendor register group 7) at VE + 0xf00: the polyphase
+ * scaler the vendor's H.265 path programs for any real downscale.  The
+ * power-of-two VE_DEC_H265_SDRT_CTRL field is NOT used in that case -- the
+ * vendor's HevcJudgeScaleMode picks mode 2 and takes this path instead.
+ *
+ * Field widths below were read out of the vendor blob and then confirmed on
+ * hardware by a write-all-ones readback: 0x10/0x18 = 3fff3fff (two 14-bit),
+ * 0x14 = ffffffff (two 16-bit), 0xf8 = 00000ffc.
+ */
+#define TOP1				0xf00
+#define TOP1_IN_SIZE			(TOP1 + 0x0c)	/* [29:16] h, [13:0] w */
+#define TOP1_OUT_SIZE			(TOP1 + 0x10)	/* [29:16] h, [13:0] w */
+#define TOP1_STEP			(TOP1 + 0x14)	/* [31:16] v, [15:0] h */
+#define TOP1_INV			(TOP1 + 0x18)	/* [29:16] v, [13:0] h */
+#define TOP1_LINE_BUF			(TOP1 + 0xe4)	/* [31:2] = addr >> 10 */
+#define TOP1_COEF_IDX			(TOP1 + 0xf8)
+#define TOP1_COEF_DATA			(TOP1 + 0xfc)
+#define TOP1_COEF_IDX_INIT		0x340
+
+/* (a << 12) / b, rounded to nearest, minus one -- exactly what the blob does. */
+static u32 cedrus_top1_ratio(u32 a, u32 b)
+{
+	if (!b)
+		return 0;
+	return (((u64)a << 12) + b / 2) / b - 1;
+}
+
+/* Filter set by downscale ratio; thresholds are the blob's, in Q12. */
+static const u16 cedrus_top1_coef_thresh[] = {
+	4608, 5120, 5632, 6144, 6656, 7168, 7680,
+	8192, 9216, 10240, 11264, 12288, 16384, 20480,
+};
+
+static unsigned int cedrus_top1_coef_set(u32 src, u32 dst)
+{
+	u32 ratio = dst ? (((u64)src << 12) + dst / 2) / dst : 4096;
+	unsigned int i;
+
+	for (i = 0; i < ARRAY_SIZE(cedrus_top1_coef_thresh); i++)
+		if (ratio < cedrus_top1_coef_thresh[i])
+			return i;
+
+	return ARRAY_SIZE(cedrus_top1_coef_thresh);
+}
+
+static void cedrus_top1_configure(struct cedrus_ctx *ctx, dma_addr_t line_buf,
+				  u32 src_w, u32 src_h, u32 out_w, u32 out_h)
+{
+	struct cedrus_dev *dev = ctx->dev;
+	unsigned int v_set = cedrus_top1_coef_set(src_h, out_h);
+	unsigned int h_set = cedrus_top1_coef_set(src_w, out_w);
+	unsigned int i;
+	u32 reg;
+
+	/*
+	 * HARDWARE-CONFIRMED FIELD ORDER: the HIGH half is horizontal/width and
+	 * the LOW half is vertical/height, in all three registers.  This is the
+	 * opposite of what the vendor disassembly appeared to say; an asymmetric
+	 * scale (2x horizontal, 4x vertical) is what settles it -- a symmetric
+	 * ratio cannot distinguish the two orders in the step registers.
+	 */
+	reg = cedrus_read(dev, TOP1_IN_SIZE) & ~0x3fff3fffu;
+	reg |= (src_w & 0x3fff) << 16 | (src_h & 0x3fff);
+	cedrus_write(dev, TOP1_IN_SIZE, reg);
+
+	reg = cedrus_read(dev, TOP1_OUT_SIZE) & ~0x3fff3fffu;
+	reg |= (out_w & 0x3fff) << 16 | (out_h & 0x3fff);
+	cedrus_write(dev, TOP1_OUT_SIZE, reg);
+
+	cedrus_write(dev, TOP1_STEP,
+		     (cedrus_top1_ratio(src_w, out_w) & 0xffff) << 16 |
+		     (cedrus_top1_ratio(src_h, out_h) & 0xffff));
+
+	reg = cedrus_read(dev, TOP1_LINE_BUF) & ~0xfffffffcu;
+	reg |= ((u32)(line_buf >> 10)) << 2;
+	cedrus_write(dev, TOP1_LINE_BUF, reg);
+
+	reg = cedrus_read(dev, TOP1_INV) & ~0x3fff3fffu;
+	reg |= (cedrus_top1_ratio(out_w, src_w) & 0x3fff) << 16 |
+	       (cedrus_top1_ratio(out_h, src_h) & 0x3fff);
+	cedrus_write(dev, TOP1_INV, reg);
+
+	reg = cedrus_read(dev, TOP1_COEF_IDX) & ~0xffcu;
+	reg |= TOP1_COEF_IDX_INIT << 2;
+	cedrus_write(dev, TOP1_COEF_IDX, reg);
+
+	/*
+	 * HORIZONTAL BANK FIRST, then vertical.  Established on hardware with a
+	 * delta/blur pair: loading delta-then-blur leaves the output sharp
+	 * across and blurred down, and swapping the two mirrors it exactly.
+	 * A symmetric ratio cannot see this -- both banks hold the same set --
+	 * so it only shows up when the axes scale by different amounts.
+	 */
+	for (i = 0; i < 32; i++)
+		cedrus_write(dev, TOP1_COEF_DATA, cedrus_scaler_coef[h_set][i]);
+	for (i = 0; i < 32; i++)
+		cedrus_write(dev, TOP1_COEF_DATA, cedrus_scaler_coef[v_set][i]);
+
+}
+
 static void cedrus_sd_program_h265(struct cedrus_ctx *ctx, dma_addr_t luma,
 				   dma_addr_t chroma)
 {
@@ -174,11 +277,40 @@
 		     VE_DEC_H265_CTRL_ROTATE_SCALE_OUT_EN |
 		     VE_DEC_H265_CTRL_DDR_CONSISTENCY_EN);
 
+	/*
+	 * scale_precision stays ZERO: the vendor's mode-2 path leaves the
+	 * power-of-two shifter off and lets the VE+0xf00 polyphase scaler do
+	 * all of the scaling.  Setting both makes them fight.
+	 */
 	cedrus_write(dev, VE_DEC_H265_SDRT_CTRL,
-		     VE_DEC_H265_SDRT_CTRL_ROTATE(cedrus_rotation(ctx)) |
-		     VE_DEC_H265_SDRT_CTRL_H_SHIFT(ctx->sd_h_shift) |
-		     VE_DEC_H265_SDRT_CTRL_V_SHIFT(ctx->sd_v_shift));
+		     VE_DEC_H265_SDRT_CTRL_ROTATE(cedrus_rotation(ctx)));
 
+	/*
+	 * 10-bit: the secondary output needs its own 2-bit plane, exactly as
+	 * the primary does.  Offsets are byte offsets into the frame, and the
+	 * 2-bit stride is the luma width / 4, 32-aligned.  Layout and bit
+	 * positions come from the vendor's regHEVC_10BIT_CONFIGURE.
+	 */
+	if (ctx->bit_depth > 8) {
+		unsigned int w = ctx->dst_fmt.width, h = ctx->dst_fmt.height;
+		unsigned int two_bit = ALIGN(w / 4, 32) * h * 3 / 2;
+		u32 reg;
+
+		cedrus_write(dev, VE_DEC_H265_OFFSET_ADDR_SECOND_OUT,
+			     ctx->dst_fmt.sizeimage - two_bit);
+
+		reg = cedrus_read(dev, VE_DEC_H265_10BIT_CONFIGURE);
+		reg &= ~(GENMASK(24, 11));
+		reg |= VE_DEC_H265_10BIT_CONFIGURE_SECOND_2BIT_STRIDE(ALIGN(w / 4, 32)) |
+		       VE_DEC_H265_10BIT_CONFIGURE_SECOND_2BIT_ENABLE |
+		       VE_DEC_H265_10BIT_CONFIGURE_SECOND_OUT_FMT(VE_DEC_H265_SECOND_OUT_FMT_8BIT_PLUS_2BIT);
+		cedrus_write(dev, VE_DEC_H265_10BIT_CONFIGURE, reg);
+	}
+
+	cedrus_top1_configure(ctx, ctx->codec.h265.scaler_buf_addr,
+			      ctx->recon_fmt.width, ctx->recon_fmt.height,
+			      ALIGN(ctx->src_fmt.width, 16) >> ctx->sd_h_shift,
+			      ALIGN(ctx->src_fmt.height, 16) >> ctx->sd_v_shift);
 }
 
 void cedrus_sd_program(struct cedrus_ctx *ctx, struct vb2_buffer *dst)
--- /dev/null
+++ b/drivers/staging/media/sunxi/cedrus/cedrus_scaler_coef.h
@@ -0,0 +1,194 @@
+/* SPDX-License-Identifier: GPL-2.0 */
+/*
+ * Polyphase coefficients for the H713 video engine scaler at VE + 0xf00.
+ *
+ * 15 sets x 32 phases x 4 taps, signed 8-bit, each phase summing to 128.
+ * A set is chosen by downscale ratio; phase p centres the kernel at tap
+ * index 1 + p/32, with the taps landing on source pixels [ip-1 .. ip+2].
+ *
+ * GENERATED, NOT EXTRACTED -- do not hand-edit.
+ *
+ * Phase 0 of the sets an exact power-of-two ratio selects is solved for
+ * that ratio rather than averaged over the set's range; at an exact power
+ * of two the phase index is always 0, so that one kernel is the whole
+ * filter.  See the generator.
+ *
+ * Run tools/video/gen-scaler-coef.py to regenerate; see that script for
+ * the kernel family, the widths, and how they were chosen.
+ */
+
+#ifndef _CEDRUS_SCALER_COEF_H_
+#define _CEDRUS_SCALER_COEF_H_
+
+#define CEDRUS_SCALER_COEF_SETS		15
+#define CEDRUS_SCALER_COEF_PHASES	32
+
+static const u32 cedrus_scaler_coef[CEDRUS_SCALER_COEF_SETS][CEDRUS_SCALER_COEF_PHASES] = {
+	/* [ 0] ratio < 1.125: cubic B-spline, width 0.64 */
+	{
+		0x00017f00, 0x00037b02, 0x00057a01, 0x00067901,
+		0x00087800, 0x000a7600, 0x000d7300, 0x00107000,
+		0x00136d00, 0x00176900, 0x001b6500, 0x00206000,
+		0x00265a00, 0x002c5400, 0x00324e00, 0x00394700,
+		0x00404000, 0x00473900, 0x004e3200, 0x00542c00,
+		0x005a2600, 0x00602000, 0x00651b00, 0x00691700,
+		0x006d1300, 0x00701000, 0x00730d00, 0x00760a00,
+		0x00780800, 0x01790600, 0x017a0500, 0x027b0300,
+	},
+	/* [ 1] ratio  < 1.25: triangle, width 1.04 */
+	{
+		0x00047705, 0x00087701, 0x000c7400, 0x00107000,
+		0x00146c00, 0x00176900, 0x001b6500, 0x001f6100,
+		0x00225e00, 0x00265a00, 0x002a5600, 0x002d5300,
+		0x00314f00, 0x00354b00, 0x00394700, 0x003c4400,
+		0x00404000, 0x00443c00, 0x00473900, 0x004b3500,
+		0x004f3100, 0x00532d00, 0x00562a00, 0x005a2600,
+		0x005e2200, 0x00611f00, 0x00651b00, 0x00691700,
+		0x006c1400, 0x00701000, 0x00740c00, 0x01770800,
+	},
+	/* [ 2] ratio < 1.375: cubic B-spline, width 0.80 */
+	{
+		0x000b6a0b, 0x000d6a09, 0x000f6908, 0x00116807,
+		0x00146705, 0x00166505, 0x00196304, 0x001d6003,
+		0x00205e02, 0x00235b02, 0x00285701, 0x002b5401,
+		0x002f5001, 0x00344c00, 0x00384800, 0x003c4400,
+		0x00404000, 0x00443c00, 0x00483800, 0x004c3400,
+		0x01502f00, 0x01542b00, 0x01572800, 0x025b2300,
+		0x025e2000, 0x03601d00, 0x04631900, 0x05651600,
+		0x05671400, 0x07681100, 0x08690f00, 0x096a0d00,
+	},
+	/* [ 3] ratio   < 1.5: cubic B-spline, width 0.92 */
+	{
+		0x00115d12, 0x00145c10, 0x00165c0e, 0x00185c0c,
+		0x001b5a0b, 0x001d590a, 0x00205808, 0x00235607,
+		0x00265406, 0x00295205, 0x002c5004, 0x002f4d04,
+		0x00334a03, 0x00364802, 0x00394502, 0x013c4201,
+		0x013f3f01, 0x01423c01, 0x02453900, 0x02483600,
+		0x034a3300, 0x044d2f00, 0x04502c00, 0x05522900,
+		0x06542600, 0x07562300, 0x08582000, 0x0a591d00,
+		0x0b5a1b00, 0x0c5c1800, 0x0e5c1600, 0x105c1400,
+	},
+	/* [ 4] ratio < 1.625: cubic B-spline, width 1.00 */
+	{
+		0x00155615, 0x00175514, 0x00195512, 0x001c5410,
+		0x001e540e, 0x0021520d, 0x0023510c, 0x0026500a,
+		0x00284f09, 0x002b4d08, 0x012d4b07, 0x01304906,
+		0x01334705, 0x01364504, 0x02384204, 0x023b4003,
+		0x033d3d03, 0x03403b02, 0x04423802, 0x04453601,
+		0x05473301, 0x06493001, 0x074b2d01, 0x084d2b00,
+		0x094e2900, 0x0a502600, 0x0c512300, 0x0d522100,
+		0x0e541e00, 0x10541c00, 0x12551900, 0x14551700,
+	},
+	/* [ 5] ratio  < 1.75: cubic B-spline, width 1.08 */
+	{
+		0x00184f19, 0x001a4f17, 0x001c4f15, 0x001f4e13,
+		0x00214e11, 0x00234d10, 0x01254c0e, 0x01274b0d,
+		0x012a490c, 0x012c480b, 0x022e460a, 0x02314508,
+		0x02334308, 0x03354107, 0x03374006, 0x04393e05,
+		0x053b3b05, 0x053e3904, 0x06403703, 0x07413503,
+		0x08433302, 0x08453102, 0x0a462e02, 0x0b482c01,
+		0x0c492a01, 0x0d4b2701, 0x0e4c2501, 0x104d2300,
+		0x114e2100, 0x134e1f00, 0x154f1c00, 0x174f1a00,
+	},
+	/* [ 6] ratio < 1.875: cubic B-spline, width 0.96 */
+	{
+		0x00135914, 0x00155912, 0x00185810, 0x001a580e,
+		0x001c570d, 0x001f560b, 0x0022540a, 0x00245309,
+		0x00275108, 0x002a4f07, 0x002d4d06, 0x00304b05,
+		0x01334804, 0x01364603, 0x01394303, 0x023b4102,
+		0x023e3e02, 0x02413b02, 0x03433901, 0x03463601,
+		0x04483301, 0x054b3000, 0x064d2d00, 0x074f2a00,
+		0x08512700, 0x09532400, 0x0a542200, 0x0b561f00,
+		0x0d571c00, 0x0e581a00, 0x10581800, 0x12591500,
+	},
+	/* [ 7] ratio   < 2.0: cubic B-spline, width 1.08 */
+	{
+		0x00184f19, 0x001a4f17, 0x001c4f15, 0x001f4e13,
+		0x00214e11, 0x00234d10, 0x01254c0e, 0x01274b0d,
+		0x012a490c, 0x012c480b, 0x022e460a, 0x02314508,
+		0x02334308, 0x03354107, 0x03374006, 0x04393e05,
+		0x053b3b05, 0x053e3904, 0x06403703, 0x07413503,
+		0x08433302, 0x08453102, 0x0a462e02, 0x0b482c01,
+		0x0c492a01, 0x0d4b2701, 0x0e4c2501, 0x104d2300,
+		0x114e2100, 0x134e1f00, 0x154f1c00, 0x174f1a00,
+	},
+	/* [ 8] ratio  < 2.25: cubic B-spline, width 1.12 */
+	{
+		0x09373709, 0x001c4c18, 0x001e4c16, 0x01204b14,
+		0x00224b13, 0x01244a11, 0x01264910, 0x0128480f,
+		0x022a470d, 0x022c460c, 0x022e450b, 0x0330430a,
+		0x03334109, 0x04344008, 0x04373e07, 0x05393c06,
+		0x053b3b05, 0x063c3905, 0x073e3704, 0x08403404,
+		0x09413303, 0x0a433003, 0x0b452e02, 0x0c462c02,
+		0x0d472a02, 0x0f482801, 0x10492601, 0x114a2401,
+		0x134b2200, 0x144b2001, 0x164c1e00, 0x184c1c00,
+	},
+	/* [ 9] ratio   < 2.5: cubic B-spline, width 1.08 */
+	{
+		0x00184f19, 0x001a4f17, 0x001c4f15, 0x001f4e13,
+		0x00214e11, 0x00234d10, 0x01254c0e, 0x01274b0d,
+		0x012a490c, 0x012c480b, 0x022e460a, 0x02314508,
+		0x02334308, 0x03354107, 0x03374006, 0x04393e05,
+		0x053b3b05, 0x053e3904, 0x06403703, 0x07413503,
+		0x08433302, 0x08453102, 0x0a462e02, 0x0b482c01,
+		0x0c492a01, 0x0d4b2701, 0x0e4c2501, 0x104d2300,
+		0x114e2100, 0x134e1f00, 0x154f1c00, 0x174f1a00,
+	},
+	/* [10] ratio  < 2.75: triangle, width 2.44 */
+	{
+		0x0a203620, 0x0a21361f, 0x0b21351f, 0x0c22341e,
+		0x0d23331d, 0x0d23331d, 0x0e24321c, 0x0f25311b,
+		0x0f26311a, 0x1026301a, 0x11272f19, 0x11282f18,
+		0x12282e18, 0x13292d17, 0x132a2d16, 0x142a2c16,
+		0x152b2b15, 0x162c2a14, 0x162d2a13, 0x172d2913,
+		0x182e2812, 0x182f2811, 0x192f2711, 0x1a302610,
+		0x1a31260f, 0x1b31250f, 0x1c32240e, 0x1d33230d,
+		0x1d33230d, 0x1e34220c, 0x1f35210b, 0x1f36210a,
+	},
+	/* [11] ratio   < 3.0: triangle, width 2.00 */
+	{
+		0x00204020, 0x01213f1f, 0x02223e1e, 0x03233d1d,
+		0x04243c1c, 0x05253b1b, 0x06263a1a, 0x07273919,
+		0x08283818, 0x09293717, 0x0a2a3616, 0x0b2b3515,
+		0x0c2c3414, 0x0d2d3313, 0x0e2e3212, 0x0f2f3111,
+		0x10303010, 0x11312f0f, 0x12322e0e, 0x13332d0d,
+		0x14342c0c, 0x15352b0b, 0x16362a0a, 0x17372909,
+		0x18382808, 0x19392707, 0x1a3a2606, 0x1b3b2505,
+		0x1c3c2404, 0x1d3d2303, 0x1e3e2202, 0x1f3f2101,
+	},
+	/* [12] ratio   < 4.0: triangle, width 2.44 */
+	{
+		0x0a203620, 0x0a21361f, 0x0b21351f, 0x0c22341e,
+		0x0d23331d, 0x0d23331d, 0x0e24321c, 0x0f25311b,
+		0x0f26311a, 0x1026301a, 0x11272f19, 0x11282f18,
+		0x12282e18, 0x13292d17, 0x132a2d16, 0x142a2c16,
+		0x152b2b15, 0x162c2a14, 0x162d2a13, 0x172d2913,
+		0x182e2812, 0x182f2811, 0x192f2711, 0x1a302610,
+		0x1a31260f, 0x1b31250f, 0x1c32240e, 0x1d33230d,
+		0x1d33230d, 0x1e34220c, 0x1f35210b, 0x1f36210a,
+	},
+	/* [13] ratio   < 5.0: triangle, width 2.44 */
+	{
+		0x39201d0a, 0x0a21361f, 0x0b21351f, 0x0c22341e,
+		0x0d23331d, 0x0d23331d, 0x0e24321c, 0x0f25311b,
+		0x0f26311a, 0x1026301a, 0x11272f19, 0x11282f18,
+		0x12282e18, 0x13292d17, 0x132a2d16, 0x142a2c16,
+		0x152b2b15, 0x162c2a14, 0x162d2a13, 0x172d2913,
+		0x182e2812, 0x182f2811, 0x192f2711, 0x1a302610,
+		0x1a31260f, 0x1b31250f, 0x1c32240e, 0x1d33230d,
+		0x1d33230d, 0x1e34220c, 0x1f35210b, 0x1f36210a,
+	},
+	/* [14] ratio  >= 5.0: triangle, width 2.44 */
+	{
+		0x381f1e0b, 0x0a21361f, 0x0b21351f, 0x0c22341e,
+		0x0d23331d, 0x0d23331d, 0x0e24321c, 0x0f25311b,
+		0x0f26311a, 0x1026301a, 0x11272f19, 0x11282f18,
+		0x12282e18, 0x13292d17, 0x132a2d16, 0x142a2c16,
+		0x152b2b15, 0x162c2a14, 0x162d2a13, 0x172d2913,
+		0x182e2812, 0x182f2811, 0x192f2711, 0x1a302610,
+		0x1a31260f, 0x1b31250f, 0x1c32240e, 0x1d33230d,
+		0x1d33230d, 0x1e34220c, 0x1f35210b, 0x1f36210a,
+	},
+};
+
+#endif /* _CEDRUS_SCALER_COEF_H_ */
